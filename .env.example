# Backend URL reachable directly from the user's browser. Required for Vercel.
# Use the verified public HTTPS URL of the deployed FastAPI backend.
# VITE_API_URL=https://your-public-backend.example.com
VITE_API_URL=

# Optional explicit fallback URLs. Do not add DHCP/LAN addresses here.
VITE_API_CANDIDATES=
