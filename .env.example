# Backend URL reachable directly from the user's browser. Required for Vercel.
# The current backend host's discovered MagicDNS name is:
# lp06.tail9b8093.ts.net
# Use the HTTPS Tailscale Serve URL in production, for example:
# VITE_API_URL=https://lp06.tail9b8093.ts.net
VITE_API_URL=

# Optional explicit fallback URLs. Do not add DHCP/LAN addresses here.
VITE_API_CANDIDATES=
